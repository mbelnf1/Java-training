package Linkedlist;

public class LinkedList { 
    Node head;
    Node newNode.add()= "The new node is: ";
    		Node head= "CurrentNode";
    	while current.next != null;
    		  current = current.next;

    		    current.next = newNode;
    		
    // utility methods
}