package daytwo;

public class BubbleSortTest {

    public static void main(String[] args) {
        List<Integer> values = new ArrayList<Integer>();

        // initialize list with unsorted values
        values = (1,2,3,4,5);
        If (values.length < 2);
        return values;
        // print unsorted list
        bubbleSort(values);
        System.out.print(values);
        
        // print sorted list
    }

    public static void bubbleSort(List<Integer> list) {
        // your implementation here
    	while (values[0] < values.length -1);
    		while (values[1] < values.length);
    			if (values[1] < values[0]);
    			temp = values[0];
    			values[0] = values[1];
    			values[1] = temp;
    			values[1] = values[1]+1;
    			values[0] = values[0]+1;
    }
}